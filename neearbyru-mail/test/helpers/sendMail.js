const nodemailer = require('nodemailer')
const { mailer } = require('../../secret/config')

class SendMail{

    constructor() {
    }

    async sendMail(text) {
        return {}
    }

}

module.exports = new SendMail();